## basic commands

# on my keyboard mod4 is the 'meta' key
set $mod Mod4
bindsym control+space exec "rofi -modi window#drun#run#ssh -show drun"
bindsym $mod+Shift+q kill
bindsym $mod+Return exec urxvt
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3?' -B 'Yes, exit i3' 'i3-msg exit'"

## initial workspace assignments

assign [class="Firefox"] → 1
# can I make it go "iff there isn't already an instance of this, then, open it here"? short of that: this is getting in th way
#assign [class="Code"] → 2
#assign [class="Signal"] → 4
#assign [class="TelegramDesktop"] 5
assign [class="qBittorrent"] → 10

#for_window [instance="mtga"] move to workspace 2
for_window [class="discord" instance="discord"] move to workspace 4
#for_window [class="TelegramDesktop"] move to workspace 5

## startup

exec_always --no-startup-id picom --experimental-backends -cb --config ~/.config/picom/picom.conf
exec --no-startup-id dunst
exec --no-startup-id feh --bg-scale $HOME/.config/feh/wallpaper.jpg
# this worked great at making the scroll wheel scroll more
# unfortunately it doesn't appear to be compatible with some things
# I ran into trouble using scroll-zoom in GIMP, for example
#exec --no-startup-id imwheel
exec --no-startup-id keepassxc
exec --no-startup-id nm-applet
exec --no-startup-id redshift-gtk
exec_always --no-startup-id "xmodmap ~/.Xmodmap"

## decorations

gaps inner 1
gaps outer 1
new_window 1pixel
new_float normal
hide_edge_borders both

## 📸 screenshots 📸

# save screenshot
bindsym Print exec --no-startup-id maim "/home/$USER/Pictures/$(date).png"
# 😎😎😎 shift + printscreen → click + drag crop tool 😎😎😎
bindsym $mod+Print exec --no-startup-id maim --window $(xdotool getactivewindow) "/home/$USER/Pictures/$(date).png"
bindsym Shift+Print exec --no-startup-id maim --select "/home/$USER/Pictures/$(date).png"

# clipboard screenshot
bindsym Ctrl+Print exec --no-startup-id maim | xclip -selection clipboard -t image/png
bindsym Ctrl+$mod+Print exec --no-startup-id maim --window $(xdotool getactivewindow) | xclip -selection clipboard -t image/png
bindsym Ctrl+Shift+Print exec --no-startup-id maim --select | xclip -selection clipboard -t image/png

## function key alt modes

# Screen Brightness for System76
bindsym $mod+F8 exec brightnessctl set 10-
bindsym $mod+F9 exec brightnessctl set +10

# Pulse Audio controls for System76
# bindsym $mod+F6 exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound
# bindsym $mod+F5 exec --no-startup-id pactl set-sink-volume 0 -5% #lower volume
# bindsym $mod+F3 exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle # mute sound

# Screen Brightness for X220
# bindsym XF86MonBrightnessUp exec light -A 5 # increase screen brightness
# bindsym XF86MonBrightnessDown exec light -U 5 # decrease screen brightness

# Pulse Audio Controls for X220
# bindsym XF86AudioRaiseVolume exec pulseaudio-control --volume-max 130 up 
# bindsym XF86AudioLowerVolume exec pulseaudio-control --volume-max 130 down 
# bindsym XF86AudioMute exec pulseaudio-control togmute # mute sound

## focus navigation

# change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

## 🗺️ navigation 🗺️

floating_modifier $mod

# ✂️ split ✂️
bindsym $mod+h split h
bindsym $mod+v split v

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows --- the default is $mod+space focus mode_toggle
bindsym $mod+break focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

## workspaces

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

## window sizing

# resize window (you can also use the mouse for that)
mode "resize" {
  # These bindings trigger as soon as you enter the resize mode

  # Pressing left will shrink the window’s width.
  # Pressing right will grow the window’s width.
  # Pressing up will shrink the window’s height.
  # Pressing down will grow the window’s height.
  bindsym Left resize shrink width 10 px or 10 ppt
  bindsym Down resize grow height 10 px or 10 ppt
  bindsym Up resize shrink height 10 px or 10 ppt
  bindsym Right resize grow width 10 px or 10 ppt

  # back to normal: Enter or Escape or $mod+r
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

## 🎨 colors 🎨

set $bg-color            #8d8daa
set $inactive-bg-color   #46496a
set $text-color          #050542
set $inactive-text-color #46496a
set $urgent-bg-color     #cf7fb3

#                       border              background         text                 indicator
client.focused          $bg-color           $bg-color          $text-color          #424242
client.unfocused        $inactive-bg-color  $inactive-bg-color $inactive-text-color #424242
client.focused_inactive $inactive-bg-color  $inactive-bg-color $inactive-text-color #424242
client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          #424242

## i3bar & i3status

set $refresh_i3status killall -SIGUSR1 i3status

bar {
  id bar-primary
  # `output` is device-specific. safe bet:
  #   run `xrandr -q | head -n 2`, then
  #   change `output` to the first word of the second line
  output HDMI-0
  separator_symbol "⚕"
  font pango:FiraCode Nerd Font 8 
  colors {
    #active_workspace  #666666 #5f676a #ffffff

    separator #444444
    background #222222
    statusline #B1B1B1

    #<colorclass>       <border> <background> <text>
    focused_workspace  #CECECE  #CECECE      #000000
    active_workspace   #333333  #333333      #888888
    inactive_workspace #CECECE  #333333      #f1f1f1
    urgent_workspace   #eb709b  #eb709b      #ffffff
  }
  status_command i3status
}

## added by "cat i3volume-pulseaudio.conf >> ~/.config/i3/config" ↓

## Volume control

# Path to volume control, without trailing slash
set $volumepath ~/scripts

# Command for the status line
#   ie: i3blocks, i3status
set $statuscmd i3status

# Signal used to update the status line
#   i3blocks uses SIGRTMIN+10 by default
#   i3status uses SIGUSR1 by default
set $statussig SIGUSR1

# Amount to increase/decrease volume as a percentage
set $volumestep 5

# pulseaudio-utils settings
#
# Symbolic name for sink (numeric index not supported)
#   Recommended: comment out this setting and omit the -s option to use default sink
#   List sink names: pacmd list-sinks | awk -F "[<>]" '/^\s+name: <.*>/{print $2}'
#set $sinkname alsa_output.pci-0000_00_1b.0.analog-stereo

# Using pulseaudio-utils (append "-s $sinkname" without quotes to override default sink)
bindsym $mod+F3 exec --no-startup-id $volumepath/volume -n -t $statuscmd -u $statussig up $volumestep
bindsym $mod+F2 exec --no-startup-id $volumepath/volume -n -t $statuscmd -u $statussig down $volumestep
bindsym $mod+F4 exec --no-startup-id $volumepath/volume -n -t $statuscmd -u $statussig mute
